import React from "react";

const Form = () => {
  return (
    <form>
      <input type="text" className="input-text" />
      <button className="button-add" type="submit">
        Add
      </button>
    </form>
  );
};

export default Form;
